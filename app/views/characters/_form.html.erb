<%= simple_form_for(@character) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end %>
